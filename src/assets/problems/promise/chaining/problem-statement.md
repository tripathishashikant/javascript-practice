# Problem: Chaining

Practice Promise chaining by transforming a value through multiple `.then()` handlers.

## Your Task

- Create a Promise that resolves `5`.
- In the first `.then()`, return `value * 2`.
- In the next `.then()`, log the final result.

## Expected Output

- `10`
