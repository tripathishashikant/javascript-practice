# Problem: Timer Delay

Create a Promise that resolves after exactly 3000ms (3 seconds).

## Your Task

- Use `setTimeout` inside the Promise executor.
- Resolve with a completion message after 3000ms.
- Handle both resolve and reject paths using `.then()` / `.catch()`.

## Expected Output

- `Hello, 3 seconds pure ho gaye!`
